"""Maintenance tooling: dependency checks and README figures.

Deliberately outside `scripts/`. That package is copied into the browser
bundle (`web/build_bundle.py`), and nothing here belongs on the page.
"""
